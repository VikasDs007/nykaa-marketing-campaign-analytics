-- 00_use_database.sql
-- Step 1: Create the database (skip if it already exists).
CREATE DATABASE IF NOT EXISTS nykaa_marketing;

-- Step 2: Use the database for all subsequent queries.
USE nykaa_marketing;
